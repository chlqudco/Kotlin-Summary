- 배열
	- 배열.. 뻔하다.
	
- 배열 정의
	- val a = emptyArray<String>()
	- val a = arrayOf("aaa","bbb")
	- val a = Array(size) { (it+1) * (it+1)}// 1부터 size 까지 정수의 제곱으로 이뤄진 배열
	- Array를 그냥 쓰지 말고 IntArray 같은 특화된 타입을 쓰자

- 배열 사용
	- 알고 있던 그대로다.
	- 별도의 배열을 만들고 싶으면 배열.copyOf() 사용
	- 문자열과 달리 == 연산자는 주소값을 비교한다
		- 내용을 비교하고 싶으면 contentEquals() 사용
